[functions]
  node_bundler = "esbuild"
  # Add the native canvas and pdf-parse modules here:
  external_node_modules = ["express", "mongoose", "@napi-rs/canvas", "pdf-parse"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true